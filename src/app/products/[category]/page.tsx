// import { redirect } from "next/navigation";

export default async function Products({} //params,
: {
	params: { pageNumber: string; category: string };
}) {
	// redirect(`/${params.category}/1`);

	return <div>TODO</div>;
}
